{% layout 'theme' %}

<div class="page-not-found center">
  <h1>{{ '404.title' | t }}</h1>
  <p>{{ '404.subtext' | t }}</p>
  <a href="{{ routes.root_url }}" class="btn">{{ '404.back_to_home' | t }}</a>
</div>
